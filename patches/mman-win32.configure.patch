diff -Naur mman-win32.old/configure mman-win32/configure
--- mman-win32.old/configure	2018-06-02 19:56:04.128188114 -0700
+++ mman-win32/configure	2018-06-02 19:57:23.769226345 -0700
@@ -35,6 +35,7 @@
   echo
   echo "  --help                    print this message"
   echo "  --prefix=PREFIX           install in PREFIX [$PREFIX]"
+  echo "  --bindir=DIR              install binaries in DIR [$PREFIX/bin]"
   echo "  --libdir=DIR              install libs in DIR [$PREFIX/lib]"
   echo "  --incdir=DIR              install includes in DIR [$PREFIX/include]"
   echo "  --enable-static           build static libraries [yes]"
@@ -88,6 +89,9 @@
     --prefix=*)
         PREFIX="$optval"
     ;;
+    --bindir=*)
+        bindir="$optval"
+    ;;
     --libdir=*)
         libdir="$optval"
     ;;
diff -Naur mman-win32.old/Makefile mman-win32/Makefile
--- mman-win32.old/Makefile	2018-06-02 19:56:04.128188114 -0700
+++ mman-win32/Makefile	2018-06-02 20:10:48.544946253 -0700
@@ -3,7 +3,7 @@
 #
 include config.mak
 
-CFLAGS=-Wall -O3 -fomit-frame-pointer
+CFLAGS:=-Wall -O3 -fomit-frame-pointer $(CFLAGS)
 
 ifeq ($(BUILD_STATIC),yes)
 	TARGETS+=libmman.a
